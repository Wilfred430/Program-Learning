#include<iostream>
using namespace std;

int main()
{
    double divisor, dividend, quotient;

    cin >> divisor >> dividend;
    quotient = divisor / dividend;
    cout << quotient;

    return 0;
}